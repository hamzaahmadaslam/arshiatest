'use strict';

/**
 * conceptulization controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::conceptulization.conceptulization');
